/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import React, { useState } from 'react';
import { validateEnvironment, EnvValidationIssue } from '../../utils/envValidation';
import { AlertCircle, ShieldAlert, CheckCircle, HelpCircle, X } from 'lucide-react';

export default function EnvValidationOverlay() {
  return null;
}
